const { /* name function for testing */ } = require(/* directory */); // example '../src/calculator'

// example
describe('Calculator', () => {
  it('should add two numbers correctly', () => {
    expect(sum(1, 2)).toBe(3);
  });
});